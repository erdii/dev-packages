
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{.package.metadata.name}}-broken"
  labels:
    app: hypershift-failing-probe-example
    instance: "{{.package.metadata.name}}"
    this-is: broken
  annotations:
    package-operator.run/phase: faulty-data
data:
  banana: bread
  apple: pie
