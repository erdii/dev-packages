apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{.package.metadata.name}}"
  labels:
    instance: "{{.package.metadata.name}}"
  annotations:
    package-operator.run/phase: configmap
data:
  config: |- {{toYAML .config | nindent 4}}
